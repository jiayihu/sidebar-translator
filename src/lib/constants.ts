/**
 * Shared constants for the Sidebar Translator extension.
 */

// ─── UI Constants ────────────────────────────────────────────────────────────

/** Number of skeleton items to show during loading */
export const SKELETON_COUNT = 5;

// ─── Timing Constants ────────────────────────────────────────────────────────

/** Debounce time for mutation observer in content script (ms) */
export const DEBOUNCE_MS = 400;

/** Debounce time for hover events in content script (ms) */
export const HOVER_DEBOUNCE_MS = 300;
